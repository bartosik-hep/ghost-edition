{{#get "posts" limit="5" filter="tags:[{{post.tags}}]+id:-{{post.id}}+tag:-hidden" as |related|}}
{{#if related}}
    <section class="related-wrapper gh-canvas">
        <h3 class="related-title">Consider also<h3>
        <div class="post-feed related-feed">
            {{#foreach posts}}
                {{> "loop"}}
            {{/foreach}}
        </div>
    </section>
{{/if}}
{{/get}}